program astest

use ratio
! ratio is the aspect ratio module  
                                 
implicit none
! implicit none

real(8):: maxi_as !, as
! defining the maximum aspect ratio

call test1 (maxi_as)
! test1 is the subroutine to get in order 4 points of every cell

print*, 'the maximum aspect ratio=', maxi_as            
! printing the maximum aspect ratio

end program astest
